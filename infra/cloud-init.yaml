#cloud-config
package_update: true
packages:
  - nginx
runcmd:
  - echo "<h1>Hola desde $(hostname)</h1>" > /var/www/html/index.nginx-debian.html
  - systemctl enable nginx
  - systemctl restart nginx